/* Complete these values before publishing. Keep the WhatsApp number digits-only. */
window.SCHOOLHUB_CONFIG = Object.freeze({
  companyName: "DualHub Technologies Uganda",
  contactEmail: "",
  whatsappNumber: "",
  formEndpoint: ""
});
